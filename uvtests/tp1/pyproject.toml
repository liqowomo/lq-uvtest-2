[project]
name = "tp1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
tp1 = "tp1:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
